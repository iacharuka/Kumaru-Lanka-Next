# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status
This repository appears to be newly initialized or empty. No source code files were found in the standard locations.

## Getting Started
When you begin adding code to this repository, consider the following:

### Common Development Commands
Once you have code in place, you'll want to establish:
- Build commands (e.g., `make`, `npm run build`, `cargo build`)
- Test commands (e.g., `npm test`, `pytest`, `go test`)
- Linting commands (e.g., `eslint`, `flake8`, `clippy`)
- Development server commands (if applicable)

### Code Organization
As you add code, consider documenting:
- Project structure and key directories
- Main entry points
- Important configuration files
- Dependency management approach
- Testing strategies

### Environment
This repository currently uses:
- Custom Anthropic base URL: https://openrouter.ai/api
- Model: nvidia/nemotron-3-super-120b-a12b:free
- See .claude/settings.json for environment configuration

When you add actual code to this repository, please update this CLAUDE.md file with specific guidance for that codebase.